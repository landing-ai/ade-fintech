# Certificate Encryption Configuration
# Copy this file to .env and update with your actual encryption key

# IMPORTANT: Keep this key secret!
# Generate a strong key using: openssl rand -base64 32
CERT_ENCRYPTION_KEY=your-secret-key-here-change-this-32-chars-long!!!

# Note: This key must match the key in assets/js/crypto-utils.js
# For production, consider using a build process to inject this key
